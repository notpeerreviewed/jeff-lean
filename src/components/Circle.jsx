// Create a circle component

const Circle = () => {
  return (
    <div
      style={{
        width: 50,
        height: 50,
        backgroundColor: "dodgerblue",
        borderRadius: "50%",
      }}
    />
  );
};

export default Circle;
